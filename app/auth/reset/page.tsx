import { Suspense } from "react";
import { ResetForm } from "@/components/dashboard/auth-card";

export default function ResetPage() {
  return (
    <Suspense>
      <ResetForm />
    </Suspense>
  );
}
